Репозиторий находится по адресу https://github.com/onlysk1ll/sf_project3

1. Требуется настроить ssh ключи на своем ПК
2. Требуется создать локальную версию данного репозитория:
git clone git@github.com:onlysk1ll/sf_project3.git
3. Требуется создать свою ветку и вносить изменения в нее

Наименование веток:
- main/master
- Develop
- Feature 
- Release 
- Hotfix 

main
Продакшен среда. Стабильный код

Develop
Разработка. 

Feature branches
Могут бранчеватся от develop
Должны вмержится в develop
Naming convention: любые названия кроме master, develop, release-*, или hotfix-*

Release branches
Могут бранчеватся от develop
Должны вмержится в develop и master
Naming convention: release-*
Помните, до того как вмержить код в релиз ветку, необходимо добавить ей тег с версией релиза (например "0.9 hotfix")

Hotfix branches
Могут бранчеватся от master
Должны вмержится в develop и master
Naming convention: hotfix-*

Репозиторий находится https://github.com/onlysk1ll/sf_project3
Для измений требуется работать по вышеуказаному git-flow
При входе требуется настроить git config, по примеру https://github.com/onlysk1ll/sf_project3/blob/master/gitconfig


Для того чтобы создать свою ветку требуется:
git branch "наименование ветки"
Для перехода в выбранную ветку:
git checkout "наименование ветки"

Внести изменение в ветку:
1. Создать изменение
2. git add .
3. git commit -m "комментарий данного изменения"

Слить эти изменения с основной кодовой базой:
1. git checkout "выбранную ветку"
2. git merge "наименование ветки", в данном случае "master"

Удачи!
